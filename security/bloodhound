# This will provide you with bloodhound test data to analyze
# To change neoj4 password
  # CALL dbms.security.changePassword('newpassword') [Enter this in the neo4j query search bar on http://localhost:7474]

# Install Package
sudo apt -y update; sudo apt -y upgrade; sudo apt -y dist-upgrade
sudo apt -y install bloodhound

# Configure Neo4j
sudo ./neo4j console
firefox http://localhost:7474
# Change password

# Generate Test Data
pip install neo4j-driver
git clone https://github.com/BloodHoundAD/BloodHound-Tools
chmod +x DBCreator.py
python DBCreator.py
#dbconfig
#connect
#generate

# Connect to Bloodhound to analyze data
