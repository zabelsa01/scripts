Find files containing passwords
ls -r -d 2 c:\path | ? {$_.FullName -like '*pass*'} | select -expand FullName

Find files with passwords in contencts of TXT and LOG Files
ls -r -d 2 -include *.txt,*.log c:\path | sls -pattern 'pass' | select Line, Path (or Out-File ./file.txt)
