If having Java issues run:
update-java-alternatives --set java-1.11.0-openjdk-amd64

sudo teamserver and run cobalt as user

if having issues connecting to cobalt from VNC try 
xhost 127.0.0.1
or
export DIAPLAY=:0

xhost si:localuser:root
xhost si:localuser:user
